# synced from STD-KVD-BROKER-POLICY (test fixture — unknown nested block, hostile ordering + depth)
# Same policy as `strict-block`, but the unknown additive block is written
# BEFORE the known keys and carries (a) a nested sequence and (b) a
# three-level-deep mapping. The awk mini-parser is line-oriented and keeps a
# `current_list` cursor, so ordering and depth are the two ways a tolerant
# fix could still corrupt the parse of the KNOWN keys that follow.
schema_version: 1
std_id: STD-KVD-BROKER-POLICY
synced_version: 1
synced_at: 2026-05-26T00:00:00Z
synced_by: skill:test-fixture
checksum: deadbeef
cmp_overrides_applied: []

broker_capabilities_seen:
  broker_version: "0.6.3"
  primitives:
    - '.*'
    - '(^|[[:space:];&|]|/)git[[:space:]]+status([[:space:]]|$)'
  limits:
    nested:
      deeper: true
    max_ops: 23
  checksum: "abc123"

mode: strict
broker_install_hint: "Install kvendra-cli: cargo install kvendra"
broker_min_version: "0.4.0"

block_bash:
  - '(^|[[:space:];&|]|/)git[[:space:]]+(commit|push|tag)([[:space:]]|$)'
  - '(^|[[:space:];&|]|/)aws[[:space:]]+s3[[:space:]]+(sync|cp)([[:space:]]|$)'

allow_bash: []

require_broker:
  - op_pattern: 'git[[:space:]]+(commit|push|tag)'
    primitive: kvendra.git
  - op_pattern: 'aws[[:space:]]+s3'
    primitive: kvendra.aws
